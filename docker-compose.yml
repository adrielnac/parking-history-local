services:
  parking-query:
    build: ./query
    volumes:
      - shared-db:/app/data
    restart: unless-stopped

  parking-ui:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - shared-db:/app/data
    restart: unless-stopped

volumes:
  shared-db:
